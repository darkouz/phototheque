<nav class="navbar navbar-expand-lg navbar-dark bg-primary">

    <a class="navbar-brand" href="{{ path('homepage') }}">Phototheque</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
            aria-controls="bs-example-navbar-collapse-1" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('homepage') }}">Home</a>
            </li>
        </ul>

        <ul class="navbar-nav" style="color:#FFF;">
            {% if is_granted("ROLE_USER") %}
                <li class="navbar-text">Bonjour {{ app.user.getFullName() }} </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('logout') }}">
                        Déconnexion
                    </a>
                </li>

            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('user_register') }}">
                        Inscription
                    </a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('login') }}">
                        Connexion
                    </a>
                </li>

            {% endif %}

        </ul>
    </div><!-- /.navbar-collapse -->

</nav>